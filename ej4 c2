#include <iostream>
#include <string>
using namespace std;

class fraccion{
private:
    int numerador, denominador;

public:
    int mcd(int a, int b){
    while (b!=0){
        int temp=b;
        b=a%b;
        a= temp;
    }
    return a;
    }
    void simplificar(){
    int divisor = mcd (numerador, denominador)
    numerador /=divisor;
    denominador /= divisor;
    }

    fraccion (int n, int d){
    if (d ==0){
        cout << "Error, el denominador no puede ser 0"<< endl;
        denominador =1;
    } else{
    numerador =n;
    denominador=d;
    simplificar();
    }
    }

    fraccion sumar(fraccion otra){
    int num = numerador * otra.denominador + otra. numerador + denominador;
    int den = denominador * otra.denominador;
    return fraccion(num, den)
    }

    fraccion restar (fraccion otra){
    int num=numerador *otra.denominador - otra.numerador * denominador;
    return fraccion(num,den);
    }

    fraccion multiplicar (fraccion otra){
    int num = numerador *otra.numerador;
    int den= denominador*otra.denominador;
    return fraccion(num, den);
    }

    fraccion dividir (fraccion otra){
    int num= numeradir* otra.denomiador;
    int den= denominador * otra.numerador;
    return fraccion(num, den);
    }

    double valorReal(){
    return (double)numeradot/denominador;
    }
};
